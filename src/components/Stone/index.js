import React from 'react';
import { Image } from 'react-native';
import styles from '../../styles/styles';
import { STONE } from '../../assets/consts';

export default Stone = () => (
    <Image style={styles.imageStone} source={STONE} />
)